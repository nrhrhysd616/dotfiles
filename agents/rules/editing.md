<!--
  ファイル編集に関する行動ルール。
  ~/.claude/rules/shared/ 経由で毎セッション全文ロードされる。
  このHTMLコメントはコンテキスト注入前に除去されるためトークンを消費しない。
-->

# ファイル編集

- 既存ファイルの文字列修正は、`sed` / `perl` の一括置換ではなく **Edit ツール**で行う。
  一括置換は意図しない箇所まで書き換えるうえ、差分が確認できない
- 新規ファイルの作成や、既存内容に触れない先頭・末尾への追記はヒアドキュメントでよい
- ファイルの移動・リネームは `git mv` を使い、履歴を保つ
